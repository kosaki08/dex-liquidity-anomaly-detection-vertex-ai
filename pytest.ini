[pytest]
addopts = -ra --import-mode=importlib --strict-markers
markers =
    unit: ユニットテスト（高速、外部依存なし）
    integration: 統合テスト（GCPが必要）
    performance: パフォーマンステスト
testpaths = tests
python_files = test_*.py
